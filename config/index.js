const appConfig = require('./app.config');
const constants = require('./constants.config');
const db = require('./database.config');
const logger = require('./logger.config');

module.exports = { appConfig, constants, db, logger };